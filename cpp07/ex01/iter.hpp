#ifndef ITER_HPP
#define ITER_HPP

template<typename T> T iter(T *addr,const int len,void (*F)(T &))
{
    if (arr = NULL || F = NULL;)
        return ;
    for (int i = 0; i < len; i++)
    {
        F(addr[i]);
    }
}

#endif